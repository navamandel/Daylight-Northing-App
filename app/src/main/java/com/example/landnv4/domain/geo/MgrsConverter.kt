package com.example.landnv4.domain.geo

import mil.nga.mgrs.MGRS
import mil.nga.mgrs.grid.GridType

object MgrsConverter {
/*
    fun latLonToMgrs(latDeg: Double, lonDeg: Double, precision: Int = 5): String {
        // precision: 1..5 gives 10km..1m (5 is common)
        return MGRS.from(latDeg, lonDeg).toString(precision)
    }

    fun mgrsToLatLon(mgrsString: String): Pair<Double, Double> {
        val mgrs = MGRS.parse(mgrsString.trim())
        val point = mgrs.toPoint()
        return point.latitude to point.longitude
    }*/
}